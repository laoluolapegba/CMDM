@model CMdm.UI.Web.Models.MdmWeight.MdmWeightModel

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            Edit Weight - @Model.WEIGHT_DESC
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink("Back To List", "Index")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn btn-warning">
                <i class="fa fa-floppy-o"></i>
                Save
            </button>
            <button type="submit" name="save-continue" class="btn bg-purple">
                <i class="fa fa-floppy-o"></i>
                SaveContinue
            </button>
            @*<span id="store-delete" class="btn bg-red">
                <i class="fa fa-trash-o"></i>
                Delete
            </span>*@
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@*@Html.DeleteConfirmation("catalog-delete")*@



